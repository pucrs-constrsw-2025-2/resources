# Exemplo de Configuração de Variáveis de Ambiente para Resources API

# MongoDB Configuration
MONGODB_INTERNAL_HOST=mongodb
MONGODB_INTERNAL_PORT=27017
RESOURCES_MONGODB_DB=resources
RESOURCES_MONGODB_USER=resources
RESOURCES_MONGODB_PASSWORD=a12345678

# Resources API Configuration
RESOURCES_INTERNAL_PROTOCOL=http
RESOURCES_INTERNAL_HOST=resources
RESOURCES_INTERNAL_API_PORT=8080
RESOURCES_INTERNAL_DEBUG_PORT=9229
RESOURCES_EXTERNAL_PROTOCOL=http
RESOURCES_EXTERNAL_HOST=localhost
RESOURCES_EXTERNAL_API_PORT=8187
RESOURCES_EXTERNAL_DEBUG_PORT=8287

# Auto-Seed Configuration
# Define como 'true' para popular o banco automaticamente na inicialização
# se o banco estiver vazio. Recomendado para ambientes de produção/staging.
AUTO_SEED=true

# Outras opções:
# AUTO_SEED=false  # Desabilita auto-seed (padrão se não definido)
# AUTO_SEED=true   # Habilita auto-seed (recomendado para produção/Docker)

# Notas:
# - O auto-seed só executa se o banco estiver completamente vazio
# - Não duplica dados em reinicializações
# - Se falhar, a aplicação continua funcionando normalmente
# - Para seed manual, use: npm run seed:mongo
